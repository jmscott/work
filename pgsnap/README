pgsnap is a simple script that dumps a source database
and then destroy/creates a target database using the source dump file.
After dumping, email is sent to the users specified in 
$PGSNAP_NOTIFY.

See the pgsnap source for details.
